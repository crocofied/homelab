apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: paperlessngx
  namespace: argocd
spec:
  project: default
  sources:
    - repoURL: ghcr.io/bjw-s-labs/helm
      chart: app-template
      targetRevision: 5.1.0
      helm:
        releaseName: paperless
        valueFiles:
          - $values/apps-manifests/media/paperless/values.yaml
    - repoURL: https://github.com/crocofied/homelab.git
      targetRevision: main
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: media
  syncPolicy:
    automated:
      prune: true
      selfHeal: true