apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nas-media
  namespace: media
spec:
  accessModes: [ReadWriteMany]
  resources:
    requests:
      storage: 2Ti
  storageClassName: ""                 
  volumeName: nas-media